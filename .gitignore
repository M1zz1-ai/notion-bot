# Secrets — never commit. Real values live in .env here, or in the master env
# file outside the repo (see .env.example).
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
